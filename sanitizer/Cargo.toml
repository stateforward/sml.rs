[package]
name = "sml-sanitizer-harness"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
sml = { package = "stateforward-sml", path = ".." }

[workspace]
members = ["."]

[lints.rust]
unsafe_code = "forbid"
unsafe_op_in_unsafe_fn = "deny"
warnings = "deny"
